```
python3 app.py
```

then visit localhost:8000
todo: add couchdb connection on port 8001
add couchdb setup scripts


create a venv:
`python -m venv .venv`

install the project:
`pip install -e .`

run tests:
`pytest -s`


package:
`pip wheel . -w wheels`
